#include <stdio.h>
#include "boolector.h"
#include <assert.h>
#include <limits.h>
#include "boolector.h"
#include "btoropt.h"

int main() {
        int result;
        BoolectorNode *array, *index1, *index2, *read1, *read2, *eq, *ne;
        BoolectorSort sort_index, sort_elem, sort_array;
        Btor *btor;
        btor = boolector_new();
    return 0;
}